# Copy to `apps/web/.env` — Vite loads .env from this directory, not the repo root.

# Base URL of apps/api. Must be an origin listed in the API's API_CORS_ORIGINS.
VITE_API_URL=http://localhost:3001
