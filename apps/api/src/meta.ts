/** Version of the API surface, reported by the health route. */
export const API_VERSION = "0.0.0";

/** Stable service identifier — clients use it to confirm what they are talking to. */
export const SERVICE_NAME = "granthalaya-api";
